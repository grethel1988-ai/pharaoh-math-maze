# 專案開發筆記 - 法老的詛咒：圖坦卡門的數學迷宮

## 📋 已完成事項

### 2026-07-10
1. **版面尺寸與自適應優化**：
   - 限制 `body` 高度為 `100vh` 且 `overflow: hidden;`，優化各元件 padding 與 margin，使遊戲各頁面完全適配單一螢幕，不產生滾動條。
2. **多頁面流程拆分**：
   - 將夥伴選擇與試煉難度選擇移動到新增的第二頁（`#mode-screen`），流程拆分為「主畫面與故事 $\rightarrow$ 夥伴與試煉選擇 $\rightarrow$ 金字塔答題」。
3. **字型與圖片尺寸放大**：
   - 大幅放大主副標題、故事背景文字、冒險夥伴頭像與卡片說明。
   - 將難度卡片的神器圖示從 `65px` 放大至 `85px`，卡片高度同步調升。
   - 題目文字放大至 `1.45rem`，選項按鈕文字放大至 `1.2rem`，按鈕前 A,B,C,D 標記放大至 `34px`，使介面極具可讀性。
4. **直立式分數顯示**：
   - 在 `game.js` 中新增分數剖析器 `formatFractions`，將題目與選項中的分數符號（如 `5/12`）自動轉換為直立分數標籤結構。
   - 在 CSS 中實作純 CSS 的直立分數排版，呈現分子在上、分母在下，中間有橫條分數線的標準格式。
5. **滿分與防重防錯鎖定**：
   - 修正點擊或 F2 快捷鍵在跳轉期間重複觸發導致得分溢出的問題，加入點擊防重機制。
   - 將 HUD 與結算畫面的得分上限強制鎖定為 100/100 分。
6. **時間單位調整**：
   - 審查並更新題庫中所有涉及時間的題目、選項與提示，將「點」一律改為「時」（如「上午 10時 40分」），符合國小中年級教學標準。
7. **本地開發者調試快捷鍵**：
   - `F2`：自動選對當前題目並前進下一題。
   - `F3`：切換無敵模式（答錯不扣火把生命）。
   - `F4`：瞬間通關，進入 100 分的勝利煙火畫面。
   - `F8`：跳題功能。支援輸入進度題號或以 `#` 開頭的原資料庫題號（例如 `#39`）精準跳轉。
   - 觸發時會在螢幕下方彈出淡金色的 Debug 通知 Toast。

### 2026-07-06
1. **GitHub 規範稽核**：
   - 檢查並確認 `index.html` 入口、相對路徑、HTTPS 混合安全性、大小寫相容性等皆符合託管標準。
2. **初始化儲存庫與基本文檔**：
   - 建立 `.gitignore`、`LICENSE` (MIT)、`README.md`、`ANTIGRAVITY.md`。
   - 本地初始化 Git，並改預設分支為 `main`。
3. **GitHub 雲端同步與部署**：
   - 建立遠端公開儲存庫 `pharaoh-math-maze` 並推送首版。
   - 啟用 GitHub Pages 部署服務，網址：https://grethel1988-ai.github.io/pharaoh-math-maze/。

## 🔍 踩坑與注意事項
* **大小寫敏感性**：GitHub Pages 伺服器區分大小寫。未來新增圖片或引資源檔案時，名稱與代碼中的路徑一律使用**全小寫**，以防網頁部署後圖片顯示破圖。
* **相對路徑**：代碼中一律不可出現 `file:///` 等本機實體路徑。
* **分數轉 HTML 顯示**：由於分數文字使用 `.innerHTML` 渲染，未來更新 `questions.js` 時需特別注意分數是否包含其他 HTML 標籤或特殊斜線字符。

## 🚀 下一步計畫
1. **專名號劃線功能**：視需求使用 python-docx 對原始 Word 文件題庫進行專名標記與標註處理。
